// Lab เพิ่มเติมของ Windows Server — 2 lab ต่อระดับ
const said = (h, re) => h.some(c => re.test(c.trim()));
const grp = (s, n) => Object.keys(s.adGroups).find(x => x.toLowerCase() === n.toLowerCase());
const inGrp = (s, g, m) => { const k = grp(s, g); return k && s.adGroups[k].includes(m); };
const ou = (s, n) => s.adOUs.some(o => String(o).toLowerCase() === n.toLowerCase());

export default {
  // ================= LEVEL 1 =================
  1: [
    {
      id: 'w1-audit',
      title: 'Lab 1B — เก็บข้อมูลเซิร์ฟเวอร์ที่รับช่วงต่อ',
      brief: 'คุณเพิ่งได้สิทธิ์เข้าเซิร์ฟเวอร์ตัวหนึ่งที่ไม่มีเอกสาร ต้องเก็บข้อมูลระบบ role ที่ติดตั้ง service ที่ทำงานอยู่ และการตั้งค่าเครือข่าย',
      device: 'windows',
      tasks: [
        { t: 'ดูข้อมูลระบบทั้งหมด (OS/CPU/RAM)', hint: 'Get-ComputerInfo', check: (s, h) => said(h, /get-computerinfo|systeminfo/i) },
        { t: 'ดูรายการ service ทั้งหมดบนเครื่อง', hint: 'Get-Service', check: (s, h) => said(h, /^(get-service|gsv)/i) },
        { t: 'ดูว่ามี Windows Feature ใดติดตั้งอยู่บ้าง', hint: 'Get-WindowsFeature', check: (s, h) => said(h, /get-windowsfeature/i) },
        { t: 'ดูการตั้งค่าเครือข่ายแบบละเอียด', hint: 'ipconfig /all', check: (s, h) => said(h, /ipconfig|get-netipconfiguration/i) },
        { t: 'ดูดิสก์และ volume ที่มี', hint: 'Get-Volume', check: (s, h) => said(h, /get-volume/i) },
        { t: 'ดู process ที่กำลังทำงาน', hint: 'Get-Process', check: (s, h) => said(h, /^(get-process|gps|ps)/i) },
        { t: 'ดู event log ฝั่ง Security ล่าสุด', hint: 'Get-EventLog -LogName Security', check: (s, h) => said(h, /get-eventlog|get-winevent/i) },
        { t: 'ดู share ที่เปิดอยู่บนเครื่อง', hint: 'Get-SmbShare', check: (s, h) => said(h, /get-smbshare|net share/i) },
      ],
    },
    {
      id: 'w1-web',
      title: 'Lab 1C — ตั้งเซิร์ฟเวอร์เว็บตัวแรก',
      brief: 'ทีมพัฒนาขอเซิร์ฟเวอร์สำหรับวางเว็บภายใน ต้องตั้งชื่อ ตั้ง IP คงที่ ติดตั้ง IIS และเปิด firewall ให้พอร์ต 80',
      device: 'windows',
      tasks: [
        { t: 'เปลี่ยนชื่อเครื่องเป็น <code>SRV-WEB01</code>', hint: 'Rename-Computer -NewName SRV-WEB01', check: s => s.hostname === 'SRV-WEB01' },
        { t: 'ตั้ง IP <code>192.168.10.40/24</code> gateway <code>192.168.10.1</code>', hint: 'New-NetIPAddress -InterfaceAlias Ethernet0 -IPAddress 192.168.10.40 -PrefixLength 24 -DefaultGateway 192.168.10.1', check: s => s.nics.Ethernet0.ip === '192.168.10.40' },
        { t: 'ตั้ง DNS เป็น <code>192.168.10.10</code>', hint: 'Set-DnsClientServerAddress -InterfaceAlias Ethernet0 -ServerAddresses 192.168.10.10', check: s => s.nics.Ethernet0.dns[0] === '192.168.10.10' },
        { t: 'ติดตั้ง role <code>Web-Server</code> (IIS)', hint: 'Install-WindowsFeature -Name Web-Server -IncludeManagementTools', check: s => s.features.has('Web-Server') },
        { t: 'ตรวจสอบว่า service <code>W3SVC</code> ทำงานอยู่', hint: 'Get-Service -Name W3SVC', check: (s, h) => said(h, /get-service.*w3svc/i) },
        { t: 'สร้าง firewall rule ชื่อ <code>Allow-HTTP</code> เปิดพอร์ต 80', hint: 'New-NetFirewallRule -DisplayName "Allow-HTTP" -Direction Inbound -Action Allow -Protocol TCP -LocalPort 80', check: s => s.fwRules.some(r => /Allow-HTTP/i.test(r.name)) },
        { t: 'สร้างโฟลเดอร์ <code>C:\\inetpub\\wwwroot\\app</code>', hint: 'New-Item -Path C:\\inetpub\\wwwroot\\app -ItemType Directory', check: s => { const w = s.fs['C:\\'].c.inetpub; return !!(w && w.c.wwwroot && w.c.wwwroot.c.app && w.c.wwwroot.c.app.d); } },
        { t: 'ทดสอบการเชื่อมต่อออกภายนอก', hint: 'Test-NetConnection 8.8.8.8', check: (s, h) => said(h, /test-netconnection|ping/i) },
      ],
    },
    {
      id: 'w1-cmdbasics',
      title: 'Lab 1F — คำสั่ง cmd พื้นฐานที่ IT ต้องใช้ทุกวัน',
      brief: 'ฝึกคำสั่ง cmd ชุดที่ใช้จริงหน้างานให้ครบ ตั้งแต่ดูข้อมูลเครือข่าย ไล่ปัญหา จัดการ process ไปจนถึงตรวจสอบระบบ — เป็นชุดคำสั่งที่ IT Support ต้องใช้เกือบทุกวัน',
      device: 'windows',
      tasks: [
        { t: 'ดูชื่อเครื่องและบัญชีที่ใช้อยู่', hint: 'hostname → whoami', check: (s, h) => said(h, /^hostname/i) && said(h, /^whoami/i) },
        { t: 'ดูข้อมูลเครือข่ายแบบละเอียด (MAC, DHCP, DNS)', hint: 'ipconfig /all', check: (s, h) => said(h, /^ipconfig\s*\/all/i) },
        { t: 'ดูตาราง routing ของเครื่อง', hint: 'route print', check: (s, h) => said(h, /^route\s+print/i) },
        { t: 'ดูตาราง ARP ว่า IP ไหนผูกกับ MAC อะไร', hint: 'arp -a', check: (s, h) => said(h, /^arp\s+-a/i) },
        { t: 'ดู MAC address ของเครื่องตัวเอง', hint: 'getmac', check: (s, h) => said(h, /^getmac/i) },
        { t: 'ทดสอบ TCP/IP stack ในเครื่อง (ping ตัวเอง)', hint: 'ping 127.0.0.1', check: (s, h) => said(h, /^ping\s+127\.0\.0\.1/i) },
        { t: 'ทดสอบว่าถึง gateway', hint: 'ping 192.168.10.1', check: (s, h) => said(h, /^ping\s+192\.168\.10\.1/i) },
        { t: 'ดูเส้นทางออกอินเทอร์เน็ตว่าผ่านที่ไหนบ้าง', hint: 'tracert 8.8.8.8', check: (s, h) => said(h, /^tracert/i) },
        { t: 'ถาม DNS ตรง ๆ ว่าชื่อนี้แปลงเป็น IP อะไร', hint: 'nslookup corp.local', check: (s, h) => said(h, /^nslookup/i) },
        { t: 'ดูว่าเครื่องจำ DNS อะไรไว้บ้าง แล้วล้างทิ้ง', hint: 'ipconfig /displaydns → ipconfig /flushdns', check: (s, h) => said(h, /ipconfig\s*\/displaydns/i) && said(h, /ipconfig\s*\/flushdns/i) },
        { t: 'ดูพอร์ตและ connection ทั้งหมดพร้อม PID', hint: 'netstat -ano', check: (s, h) => said(h, /^netstat\s+-ano/i) },
        { t: 'ดูรายการ process ทั้งหมด', hint: 'tasklist', check: (s, h) => said(h, /^tasklist/i) },
        { t: 'ปิด process <code>spoolsv.exe</code> ด้วย taskkill', hint: 'taskkill /IM spoolsv.exe', check: s => !s.processes.some(p => p.name === 'spoolsv.exe') },
        { t: 'ดูสถานะ service ด้วย <code>sc query</code>', hint: 'sc query WinRM', check: (s, h) => said(h, /^sc\s+query/i) },
        { t: 'ดูข้อมูลระบบทั้งหมด', hint: 'systeminfo', check: (s, h) => said(h, /^systeminfo/i) },
        { t: 'ดูการตั้งค่า IP ผ่าน netsh', hint: 'netsh interface ip show config', check: (s, h) => said(h, /netsh\s+interface\s+ip\s+show/i) },
        { t: 'เปลี่ยน IP เป็น <code>192.168.10.60</code> ด้วย netsh', hint: 'netsh interface ip set address "Ethernet0" static 192.168.10.60 255.255.255.0 192.168.10.1', check: s => s.nics.Ethernet0.ip === '192.168.10.60' },
        { t: 'ดูรายการ scheduled task', hint: 'schtasks /query', check: (s, h) => said(h, /^schtasks\s*\/query/i) },
        { t: 'ตรวจพื้นที่ดิสก์ด้วย wmic', hint: 'wmic logicaldisk get', check: (s, h) => said(h, /^wmic/i) },
        { t: 'ดูไฟล์ในไดเรกทอรี C:\\ ด้วย dir', hint: 'dir C:\\', check: (s, h) => said(h, /^dir/i) },
      ],
    },
  ],

  // ================= LEVEL 2 =================
  2: [
    {
      id: 'w2-bulk',
      title: 'Lab 2B — จัดโครงสร้าง OU และผู้ใช้ทั้งแผนก',
      brief: 'HR ส่งรายชื่อพนักงานใหม่ 4 คนจาก 2 แผนก ต้องสร้าง OU ตามแผนก สร้างบัญชี และจัดกลุ่มตามหลัก AGDLP',
      device: 'windows',
      tasks: [
        { t: 'ติดตั้ง role AD และสร้าง forest <code>corp.local</code>', hint: 'Install-WindowsFeature -Name AD-Domain-Services -IncludeManagementTools → Install-ADDSForest -DomainName corp.local', check: s => s.domain === 'corp.local' },
        { t: 'สร้าง OU <code>Sales</code>', hint: 'New-ADOrganizationalUnit -Name Sales', check: s => ou(s, 'Sales') },
        { t: 'สร้าง OU <code>Finance</code>', hint: 'New-ADOrganizationalUnit -Name Finance', check: s => ou(s, 'Finance') },
        { t: 'สร้าง user <code>somsak</code> และ <code>malee</code> (ฝ่ายขาย) แบบเปิดใช้งาน', hint: 'New-ADUser -Name "Somsak K" -SamAccountName somsak -Enabled $true → New-ADUser -Name "Malee T" -SamAccountName malee -Enabled $true', check: s => s.adUsers.somsak && s.adUsers.malee && s.adUsers.somsak.enabled && s.adUsers.malee.enabled },
        { t: 'สร้าง user <code>wichai</code> และ <code>preeda</code> (ฝ่ายบัญชี) แบบเปิดใช้งาน', hint: 'New-ADUser -Name "Wichai S" -SamAccountName wichai -Enabled $true → New-ADUser -Name "Preeda N" -SamAccountName preeda -Enabled $true', check: s => s.adUsers.wichai && s.adUsers.preeda },
        { t: 'สร้างกลุ่ม <code>GG-Sales</code> (Global group ตามหน้าที่)', hint: 'New-ADGroup -Name GG-Sales -GroupScope Global', check: s => !!grp(s, 'GG-Sales') },
        { t: 'สร้างกลุ่ม <code>GG-Finance</code>', hint: 'New-ADGroup -Name GG-Finance -GroupScope Global', check: s => !!grp(s, 'GG-Finance') },
        { t: 'เพิ่ม <code>somsak</code> และ <code>malee</code> เข้า <code>GG-Sales</code>', hint: 'Add-ADGroupMember -Identity GG-Sales -Members somsak → Add-ADGroupMember -Identity GG-Sales -Members malee', check: s => inGrp(s, 'GG-Sales', 'somsak') && inGrp(s, 'GG-Sales', 'malee') },
        { t: 'เพิ่ม <code>wichai</code> และ <code>preeda</code> เข้า <code>GG-Finance</code>', hint: 'Add-ADGroupMember -Identity GG-Finance -Members wichai → Add-ADGroupMember -Identity GG-Finance -Members preeda', check: s => inGrp(s, 'GG-Finance', 'wichai') && inGrp(s, 'GG-Finance', 'preeda') },
        { t: 'ตรวจสอบรายชื่อผู้ใช้ทั้งหมด', hint: 'Get-ADUser -Filter *', check: (s, h) => said(h, /get-aduser/i) },
      ],
    },
    {
      id: 'w2-dnsdhcp',
      title: 'Lab 2C — DNS Zone และ DHCP ครบวงจร',
      brief: 'หลังตั้งโดเมนเสร็จ ต้องตั้ง DNS zone สำหรับเซิร์ฟเวอร์ภายใน และ DHCP ที่แจก gateway/DNS ถูกต้อง พร้อมจอง IP ให้เครื่องพิมพ์',
      device: 'windows',
      tasks: [
        { t: 'สร้าง forest <code>corp.local</code>', hint: 'Install-WindowsFeature -Name AD-Domain-Services -IncludeManagementTools → Install-ADDSForest -DomainName corp.local', check: s => s.domain === 'corp.local' },
        { t: 'ติดตั้ง role <code>DNS</code>', hint: 'Install-WindowsFeature -Name DNS -IncludeManagementTools', check: s => s.features.has('DNS') },
        { t: 'สร้าง DNS zone <code>lab.local</code>', hint: 'Add-DnsServerPrimaryZone -Name lab.local -ReplicationScope Domain', check: s => s.dnsZones.some(z => z.name === 'lab.local') },
        { t: 'เพิ่ม A record <code>srv-file01</code> → <code>192.168.10.20</code>', hint: 'Add-DnsServerResourceRecordA -Name srv-file01 -ZoneName lab.local -IPv4Address 192.168.10.20', check: s => s.dnsRecords.some(r => r.name === 'srv-file01' && r.data === '192.168.10.20') },
        { t: 'เพิ่ม A record <code>srv-app01</code> → <code>192.168.10.30</code>', hint: 'Add-DnsServerResourceRecordA -Name srv-app01 -ZoneName lab.local -IPv4Address 192.168.10.30', check: s => s.dnsRecords.some(r => r.name === 'srv-app01') },
        { t: 'ติดตั้ง role <code>DHCP</code>', hint: 'Install-WindowsFeature -Name DHCP -IncludeManagementTools', check: s => s.features.has('DHCP') },
        { t: 'Authorize DHCP server ใน AD', hint: 'Add-DhcpServerInDC', check: s => s.dhcpAuthorized },
        { t: 'สร้าง scope <code>LAN-Office</code> ช่วง <code>192.168.10.100-192.168.10.200</code>', hint: 'Add-DhcpServerv4Scope -Name LAN-Office -StartRange 192.168.10.100 -EndRange 192.168.10.200 -SubnetMask 255.255.255.0', check: s => s.dhcpScopes.some(x => x.name === 'LAN-Office') },
        { t: 'ตั้ง option: Router <code>192.168.10.1</code> และ DNS <code>192.168.10.10</code>', hint: 'Set-DhcpServerv4OptionValue -ScopeId 192.168.10.0 -Router 192.168.10.1 -DnsServer 192.168.10.10', check: s => s.dhcpScopes.some(x => x.router === '192.168.10.1' && x.dns === '192.168.10.10') },
        { t: 'จอง IP <code>192.168.10.50</code> ให้เครื่องพิมพ์ MAC <code>00-0C-29-11-22-33</code>', hint: 'Add-DhcpServerv4Reservation -ScopeId 192.168.10.0 -IPAddress 192.168.10.50 -ClientId 00-0C-29-11-22-33 -Description Printer-FL2', check: s => s.dhcpReservations.some(r => r.ip === '192.168.10.50' && /00-0C-29-11-22-33/i.test(r.mac || '')) },
        { t: 'ตรวจสอบ record ที่สร้างไว้', hint: 'Get-DnsServerResourceRecord -ZoneName lab.local', check: (s, h) => said(h, /get-dnsserverresourcerecord/i) },
      ],
    },
  ],

  // ================= LEVEL 3 =================
  3: [
    {
      id: 'w3-gpo',
      title: 'Lab 3B — สร้างและ Link Group Policy',
      brief: 'ฝ่ายความปลอดภัยขอให้บังคับนโยบายรหัสผ่านกับทุกคน และล็อกหน้าจอกับเครื่องในแผนกไอทีเป็นการเฉพาะ',
      device: 'windows',
      tasks: [
        { t: 'สร้าง forest <code>corp.local</code>', hint: 'Install-WindowsFeature -Name AD-Domain-Services -IncludeManagementTools → Install-ADDSForest -DomainName corp.local', check: s => s.domain === 'corp.local' },
        { t: 'สร้าง OU <code>IT</code>', hint: 'New-ADOrganizationalUnit -Name IT', check: s => ou(s, 'IT') },
        { t: 'สร้าง OU <code>Workstations</code>', hint: 'New-ADOrganizationalUnit -Name Workstations', check: s => ou(s, 'Workstations') },
        { t: 'สร้าง GPO ชื่อ <code>Password-Policy</code>', hint: 'New-GPO -Name "Password-Policy"', check: s => s.gpos.includes('Password-Policy') },
        { t: 'สร้าง GPO ชื่อ <code>Screen-Lock</code>', hint: 'New-GPO -Name "Screen-Lock"', check: s => s.gpos.includes('Screen-Lock') },
        { t: 'Link <code>Password-Policy</code> ไปที่ระดับโดเมน <code>DC=corp,DC=local</code>', hint: 'New-GPLink -Name "Password-Policy" -Target "DC=corp,DC=local"', check: s => s.gpLinks.some(l => l.gpo === 'Password-Policy' && /DC=corp/i.test(l.target)) },
        { t: 'Link <code>Screen-Lock</code> ไปที่ <code>OU=IT,DC=corp,DC=local</code>', hint: 'New-GPLink -Name "Screen-Lock" -Target "OU=IT,DC=corp,DC=local"', check: s => s.gpLinks.some(l => l.gpo === 'Screen-Lock' && /OU=IT/i.test(l.target)) },
        { t: 'ตรวจสอบรายการ GPO ทั้งหมด', hint: 'Get-GPO', check: (s, h) => said(h, /get-gpo/i) },
        { t: 'ตรวจสอบ link ที่สร้างไว้', hint: 'Get-GPLink', check: (s, h) => said(h, /get-gplink/i) },
        { t: 'บังคับดึง policy ใหม่ที่เครื่อง', hint: 'gpupdate /force', check: (s, h) => said(h, /gpupdate/i) },
        { t: 'ดูว่า GPO ใดถูก apply กับผู้ใช้ปัจจุบัน', hint: 'gpresult /r', check: (s, h) => said(h, /gpresult/i) },
      ],
    },
    {
      id: 'w3-shares',
      title: 'Lab 3C — File Server หลายแผนก',
      brief: 'ต้องเตรียม share ให้ 3 แผนก โดยแต่ละแผนกเห็นเฉพาะโฟลเดอร์ของตัวเอง และมี share กลางสำหรับไฟล์ที่ใช้ร่วมกัน',
      device: 'windows',
      tasks: [
        { t: 'สร้าง forest <code>corp.local</code>', hint: 'Install-WindowsFeature -Name AD-Domain-Services -IncludeManagementTools → Install-ADDSForest -DomainName corp.local', check: s => s.domain === 'corp.local' },
        { t: 'สร้างโฟลเดอร์ <code>C:\\Shares</code>', hint: 'New-Item -Path C:\\Shares -ItemType Directory', check: s => !!s.fs['C:\\'].c.Shares },
        { t: 'สร้างโฟลเดอร์ย่อย <code>Sales</code>, <code>Finance</code> และ <code>Common</code>', hint: 'New-Item -Path C:\\Shares\\Sales -ItemType Directory → New-Item -Path C:\\Shares\\Finance -ItemType Directory → New-Item -Path C:\\Shares\\Common -ItemType Directory', check: s => { const sh = s.fs['C:\\'].c.Shares; return sh && sh.c.Sales && sh.c.Finance && sh.c.Common; } },
        { t: 'สร้างกลุ่ม <code>DL-Sales-RW</code> (Domain Local ตามทรัพยากร)', hint: 'New-ADGroup -Name DL-Sales-RW -GroupScope DomainLocal', check: s => !!grp(s, 'DL-Sales-RW') },
        { t: 'สร้างกลุ่ม <code>DL-Finance-RW</code>', hint: 'New-ADGroup -Name DL-Finance-RW -GroupScope DomainLocal', check: s => !!grp(s, 'DL-Finance-RW') },
        { t: 'สร้าง share <code>Sales</code> ชี้ไปที่ <code>C:\\Shares\\Sales</code>', hint: 'New-SmbShare -Name Sales -Path C:\\Shares\\Sales -FullAccess Everyone', check: s => s.shares.Sales && /Sales/i.test(s.shares.Sales.path) },
        { t: 'สร้าง share <code>Finance</code>', hint: 'New-SmbShare -Name Finance -Path C:\\Shares\\Finance -FullAccess Everyone', check: s => !!s.shares.Finance },
        { t: 'สร้าง share <code>Common</code>', hint: 'New-SmbShare -Name Common -Path C:\\Shares\\Common -FullAccess Everyone', check: s => !!s.shares.Common },
        { t: 'ตรวจสอบรายการ share ทั้งหมด', hint: 'Get-SmbShare', check: (s, h) => said(h, /get-smbshare/i) },
        { t: 'ติดตั้ง feature <code>Windows-Server-Backup</code> เตรียมสำรองข้อมูล', hint: 'Install-WindowsFeature -Name Windows-Server-Backup', check: s => s.features.has('Windows-Server-Backup') },
      ],
    },
  ],

  // ================= LEVEL 4 =================
  4: [
    {
      id: 'w4-hyperv',
      title: 'Lab 4B — สร้าง Virtual Switch และ VM',
      brief: 'ทีมขอเครื่องทดสอบ 2 เครื่อง คุณต้องเตรียม Hyper-V host ให้พร้อม สร้าง virtual switch แล้วสร้าง VM ตามสเปกที่ขอ',
      device: 'windows',
      tasks: [
        { t: 'เปลี่ยนชื่อเครื่องเป็น <code>SRV-HV02</code>', hint: 'Rename-Computer -NewName SRV-HV02', check: s => s.hostname === 'SRV-HV02' },
        { t: 'ติดตั้ง role <code>Hyper-V</code>', hint: 'Install-WindowsFeature -Name Hyper-V -IncludeManagementTools', check: s => s.features.has('Hyper-V') },
        { t: 'สร้าง virtual switch แบบ External ชื่อ <code>vSwitch-LAN</code>', hint: 'New-VMSwitch -Name vSwitch-LAN -NetAdapterName Ethernet0', check: s => s.vmSwitches.some(x => x.name === 'vSwitch-LAN' && x.type === 'External') },
        { t: 'สร้าง virtual switch แบบ Private ชื่อ <code>vSwitch-Isolated</code>', hint: 'New-VMSwitch -Name vSwitch-Isolated -SwitchType Private', check: s => s.vmSwitches.some(x => x.name === 'vSwitch-Isolated' && x.type === 'Private') },
        { t: 'สร้าง VM <code>TEST-APP01</code> RAM 4GB Generation 2 ต่อกับ <code>vSwitch-LAN</code>', hint: 'New-VM -Name TEST-APP01 -MemoryStartupBytes 4GB -Generation 2 -SwitchName vSwitch-LAN', check: s => s.vms.some(v => v.name === 'TEST-APP01' && v.sw === 'vSwitch-LAN') },
        { t: 'สร้าง VM <code>TEST-DB01</code> ต่อกับ <code>vSwitch-Isolated</code>', hint: 'New-VM -Name TEST-DB01 -MemoryStartupBytes 4GB -Generation 2 -SwitchName vSwitch-Isolated', check: s => s.vms.some(v => v.name === 'TEST-DB01' && v.sw === 'vSwitch-Isolated') },
        { t: 'เปิดเครื่อง <code>TEST-APP01</code>', hint: 'Start-VM -Name TEST-APP01', check: s => s.vms.some(v => v.name === 'TEST-APP01' && v.state === 'Running') },
        { t: 'ตรวจสอบรายการ VM', hint: 'Get-VM', check: (s, h) => said(h, /get-vm\b/i) },
        { t: 'ตรวจสอบรายการ virtual switch', hint: 'Get-VMSwitch', check: (s, h) => said(h, /get-vmswitch/i) },
      ],
    },
    {
      id: 'w4-harden',
      title: 'Lab 4C — Hardening เซิร์ฟเวอร์ตาม Security Baseline',
      brief: 'ผลการตรวจสอบพบว่าเซิร์ฟเวอร์ยังเปิด SMBv1 และไม่มี firewall rule ที่ควบคุมชัดเจน ต้องแก้ตาม baseline และเตรียมงานตรวจสอบอัตโนมัติ',
      device: 'windows',
      tasks: [
        { t: 'ตรวจสอบสถานะ SMB ปัจจุบัน', hint: 'Get-SmbServerConfiguration', check: (s, h) => said(h, /get-smbserverconfiguration/i) },
        { t: 'ปิด <code>SMB1Protocol</code>', hint: 'Disable-WindowsOptionalFeature -Online -FeatureName SMB1Protocol', check: s => s.smb1 === false },
        { t: 'ตรวจสอบ firewall profile ทั้ง 3 โปรไฟล์', hint: 'Get-NetFirewallProfile', check: (s, h) => said(h, /get-netfirewallprofile/i) },
        { t: 'สร้าง rule <code>Allow-RDP-Mgmt</code> เปิดพอร์ต 3389', hint: 'New-NetFirewallRule -DisplayName "Allow-RDP-Mgmt" -Direction Inbound -Action Allow -Protocol TCP -LocalPort 3389', check: s => s.fwRules.some(r => /Allow-RDP-Mgmt/i.test(r.name)) },
        { t: 'สร้าง rule <code>Block-Legacy-SMB</code> แบบ Block พอร์ต 445', hint: 'New-NetFirewallRule -DisplayName "Block-Legacy-SMB" -Direction Inbound -Action Block -Protocol TCP -LocalPort 445', check: s => s.fwRules.some(r => /Block-Legacy-SMB/i.test(r.name) && /block/i.test(r.action)) },
        { t: 'ปิด service <code>TermService</code> ที่ไม่ได้ใช้ (ตั้ง StartupType เป็น Disabled)', hint: 'Set-Service -Name TermService -StartupType Disabled', check: s => /disabled/i.test(s.services.TermService.start) },
        { t: 'สร้าง local user <code>svc-monitor</code> สำหรับระบบเฝ้าระวัง', hint: 'New-LocalUser -Name svc-monitor -Description "Monitoring service account"', check: s => !!s.localUsers['svc-monitor'] },
        { t: 'ดู event log ฝั่ง Security เพื่อตรวจการล็อกอินล้มเหลว', hint: 'Get-EventLog -LogName Security', check: (s, h) => said(h, /get-eventlog|get-winevent/i) },
        { t: 'สร้าง scheduled task ชื่อ <code>DailySecurityCheck</code>', hint: 'New-ScheduledTask -TaskName DailySecurityCheck', check: s => s.scheduledTasks.some(t2 => t2.name === 'DailySecurityCheck') },
        { t: 'ตรวจสอบ firewall rule ที่สร้างไว้', hint: 'Get-NetFirewallRule', check: (s, h) => said(h, /get-netfirewallrule/i) },
      ],
    },
  ],

  // ================= LEVEL 5 =================
  5: [
    {
      id: 'w5-sites',
      title: 'Lab 5B — AD Sites, Subnets และ Recycle Bin',
      brief: 'องค์กรเปิดสาขาเชียงใหม่ พนักงานสาขาบ่นว่าล็อกอินช้ามาก สาเหตุคือยังไม่ได้ประกาศ site/subnet ทำให้ client ข้ามไปหา DC ที่กรุงเทพ',
      device: 'windows',
      tasks: [
        { t: 'สร้าง forest <code>corp.local</code>', hint: 'Install-WindowsFeature -Name AD-Domain-Services -IncludeManagementTools → Install-ADDSForest -DomainName corp.local', check: s => s.domain === 'corp.local' },
        { t: 'ตรวจสอบข้อมูล forest', hint: 'Get-ADForest', check: (s, h) => said(h, /get-adforest/i) },
        { t: 'เปิด AD Recycle Bin (ควรทำทันทีหลังสร้างโดเมน)', hint: 'Enable-ADOptionalFeature -Identity "Recycle Bin Feature" -Scope ForestOrConfigurationSet -Target corp.local', check: s => s.recycleBin === true },
        { t: 'สร้าง site <code>BKK-HQ</code>', hint: 'New-ADReplicationSite -Name BKK-HQ', check: s => s.adSites.includes('BKK-HQ') },
        { t: 'สร้าง site <code>CNX-Branch</code>', hint: 'New-ADReplicationSite -Name CNX-Branch', check: s => s.adSites.includes('CNX-Branch') },
        { t: 'ประกาศ subnet <code>192.168.10.0/24</code> ให้ site <code>BKK-HQ</code>', hint: 'New-ADReplicationSubnet -Name 192.168.10.0/24 -Site BKK-HQ', check: s => s.adSubnets.some(x => x.name === '192.168.10.0/24' && x.site === 'BKK-HQ') },
        { t: 'ประกาศ subnet <code>192.168.20.0/24</code> ให้ site <code>CNX-Branch</code>', hint: 'New-ADReplicationSubnet -Name 192.168.20.0/24 -Site CNX-Branch', check: s => s.adSubnets.some(x => x.name === '192.168.20.0/24' && x.site === 'CNX-Branch') },
        { t: 'ตรวจสอบรายการ subnet ที่ประกาศไว้', hint: 'Get-ADReplicationSubnet', check: (s, h) => said(h, /get-adreplicationsubnet/i) },
        { t: 'ตรวจสอบว่า Recycle Bin เปิดแล้ว', hint: 'Get-ADOptionalFeature', check: (s, h) => said(h, /get-adoptionalfeature/i) },
      ],
    },
    {
      id: 'w5-svc',
      title: 'Lab 5C — Service Account และการแบ่งสิทธิ์',
      brief: 'ระบบใหม่ 3 ตัวต้องการ service account แยกกัน และทีม helpdesk ต้องมีสิทธิ์เฉพาะที่จำเป็นเท่านั้น ห้ามใช้ Domain Admin ทำงานประจำวัน',
      device: 'windows',
      tasks: [
        { t: 'สร้าง forest <code>corp.local</code>', hint: 'Install-WindowsFeature -Name AD-Domain-Services -IncludeManagementTools → Install-ADDSForest -DomainName corp.local', check: s => s.domain === 'corp.local' },
        { t: 'สร้าง OU <code>ServiceAccounts</code>', hint: 'New-ADOrganizationalUnit -Name ServiceAccounts', check: s => ou(s, 'ServiceAccounts') },
        { t: 'สร้าง OU <code>AdminAccounts</code>', hint: 'New-ADOrganizationalUnit -Name AdminAccounts', check: s => ou(s, 'AdminAccounts') },
        { t: 'สร้าง service account <code>svc-sql</code> แบบเปิดใช้งาน', hint: 'New-ADUser -Name "SQL Service" -SamAccountName svc-sql -Enabled $true', check: s => s.adUsers['svc-sql'] && s.adUsers['svc-sql'].enabled },
        { t: 'สร้าง service account <code>svc-backup</code>', hint: 'New-ADUser -Name "Backup Service" -SamAccountName svc-backup -Enabled $true', check: s => !!s.adUsers['svc-backup'] },
        { t: 'สร้าง service account <code>svc-monitor</code>', hint: 'New-ADUser -Name "Monitor Service" -SamAccountName svc-monitor -Enabled $true', check: s => !!s.adUsers['svc-monitor'] },
        { t: 'สร้างบัญชีผู้ดูแล <code>hd-somchai</code> สำหรับ helpdesk', hint: 'New-ADUser -Name "Helpdesk Somchai" -SamAccountName hd-somchai -Enabled $true', check: s => !!s.adUsers['hd-somchai'] },
        { t: 'สร้างกลุ่ม <code>GG-Helpdesk</code>', hint: 'New-ADGroup -Name GG-Helpdesk -GroupScope Global', check: s => !!grp(s, 'GG-Helpdesk') },
        { t: 'เพิ่ม <code>hd-somchai</code> เข้ากลุ่ม <code>GG-Helpdesk</code>', hint: 'Add-ADGroupMember -Identity GG-Helpdesk -Members hd-somchai', check: s => inGrp(s, 'GG-Helpdesk', 'hd-somchai') },
        { t: 'สร้างกลุ่ม <code>GG-ServiceAccounts</code> และเพิ่ม <code>svc-sql</code>', hint: 'New-ADGroup -Name GG-ServiceAccounts -GroupScope Global → Add-ADGroupMember -Identity GG-ServiceAccounts -Members svc-sql', check: s => inGrp(s, 'GG-ServiceAccounts', 'svc-sql') },
        { t: 'ตรวจสอบสมาชิกของ <code>GG-Helpdesk</code>', hint: 'Get-ADGroupMember -Identity GG-Helpdesk', check: (s, h) => said(h, /get-adgroupmember/i) },
      ],
    },
  ],
};
